/**
 * SIMULATION PACKAGE - Full GEN0 Rendering
 * React Three Fiber visualization of planetary genesis
 */

import React from 'react';
import ReactDOM from 'react-dom/client';
import { App } from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

